# Leave empty to use Vite dev proxy to the API (see vite.config.js).
# For production builds served separately from the API, set the full origin:
# VITE_API_URL=https://api.example.com

VITE_API_URL=

# Supabase Auth (required for login/signup UI)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
